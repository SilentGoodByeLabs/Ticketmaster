{
  "name": "Ticketmaster",
  "version": "1.0.0",
  "description": "Ticketmaster",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo 'No build step needed - using www directory'",
    "android": "npx cap add android && npx cap sync android",
    "ios": "npx cap add ios && npx cap sync ios"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
